# Fraud-detection-XGBoost
Developed an end-to-end fraud detection project using the IEEE-CIS dataset, including data cleaning, feature encoding, and class imbalance handling with SMOTE. Trained an XGBoost model and evaluated performance using confusion matrix and classification metrics.
